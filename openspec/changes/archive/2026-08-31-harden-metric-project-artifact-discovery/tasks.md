## 1. Project ownership evidence

- [x] 1.1 Preserve duplicate project-output simple-name evidence in the session metadata index and require it for metric project-owner trust; verify the focused Core metric tests pass.
- [x] 1.2 Request existing project-output evidence for ordinary explicit-target measurement when configured metrics require canonical project ownership; verify runner-setup tests prove the flag and resulting contributor.

## 2. Regression coverage

- [x] 2.1 Add a metric regression for two distinct project artifacts with one output assembly simple name and verify the metric is unassessable rather than dependent on discovery order.
- [x] 2.2 Add an explicit-target/project-metric runner integration regression and verify it produces the normalized project-path contributor without build preparation.

## 3. Validation and lifecycle

- [x] 3.1 Run focused Core metric and runner-setup tests plus applicable repository checks, and verify all pass.
- [x] 3.2 Archive the OpenSpec change and verify `openspec validate --all` passes.
