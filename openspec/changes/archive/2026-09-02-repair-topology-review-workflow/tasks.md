## 1. Core boundary and approved API

- [x] 1.1 Project ordinary evaluator observations into a neutral Validation-owned topology DTO and remove all Topology-to-Execution references.
- [x] 1.2 Internalize the capture service composition seam, add the Topology namespace to self-policy inventory, and verify protected-contract tests.
- [x] 1.3 Regenerate and review both canonical Core public-API approval baselines.

## 2. CLI safety and diagnostics

- [x] 2.1 Build one trusted-input manifest and reject output aliases using filesystem identity rather than textual paths.
- [x] 2.2 Publish topology documents through temporary files and atomic replacement while preserving target content on failure.
- [x] 2.3 Resolve parse-error usage hints from command ancestry and cover capture, diff, and verify.
- [x] 2.4 Reuse the normal validation request and external-evidence execution semantics in topology diff and verify.

## 3. Automated lifecycle evidence

- [x] 3.1 Build the .NET fixture and materialize Unity-style assemblies in `Library/ScriptAssemblies` during NUnit setup.
- [x] 3.2 Exercise real capture, diff, and verify commands for both fixtures, including byte stability, all topology categories, strict/audit exits, and input hashes.
- [x] 3.3 Add alias and failed-publication regression tests.

## 4. Validation and delivery

- [x] 4.1 Run focused Core and CLI test suites plus public API and self-architecture gates.
- [x] 4.2 Run format/lint and the proportionate full validation suite; record any external CI-only coverage.
- [x] 4.3 Validate and archive the OpenSpec repair change, commit it, and update PR #758.
