## 1. Reference-policy evidence

- [x] 1.1 Add synthetic server-style and library/runtime-editor fixture types with user-owned API-membership, semantic-role, version, marker-placement, and recursive-wrapper evidence; verify the Core test project compiles them without new dependencies.
- [x] 1.2 Add a fully declarative Core reference-policy test that composes existing strict/audit attribute-usage, public-API-surface, contract-surface-exposure, and versioned-isolation contracts; verify deterministic strict/audit findings, recursive first-party path evidence, preserved primary role, bounded non-annotation selection, and Human/JSON/SARIF/Testing projections.

## 2. Contract authoring guidance

- [x] 2.1 Document the recommended responsibility split and composition of marker placement, intentional API membership, recursive exposure, and version isolation on the existing contract pages; verify examples only use supported schema fields and preserve the strict/audit/static-analysis boundaries.

## 3. Integration and lifecycle

- [x] 3.1 Inspect the integrated diff and run the focused Core tests, `make fmt`, relevant policy/docs lint, and OpenSpec validation; verify no unrelated change or formatting drift remains.
- [x] 3.2 Synchronize the completed task state and archive the OpenSpec change; verify `openspec validate --all` passes before opening the pull request.
