## 1. Extract metric-kind calculations

- [x] 1.1 Introduce an internal raw calculation-result seam and extract topology type-count,
  footprint, and direct component-relation contributors into a purpose-named calculator; verify
  topology metric and completeness tests preserve contributor values and reason codes.
- [x] 1.2 Extract external dependency-group source-identity/contributor calculation and public
  contract-surface assembly-resolution/scanner reuse into purpose-named calculators; verify
  external-group, project-ownership, and public-surface metric tests pass.
- [x] 1.3 Rewire `ArchitectureMetricEvaluator` as the sole selected-definition, applicability,
  normalization, and outcome coordinator; verify `ArchitectureMetricEvaluator.cs` is below 500
  lines and metric-budget tests continue to reuse one measurement outcome.

## 2. Prove and record the refactor

- [x] 2.1 Add or move focused collaborator-seam tests for topology success/incomplete evidence,
  external source-identity recovery, and public-contract success/missing evidence while retaining
  outcome-level metric regression coverage; verify focused Core metric/applicability/budget test
  families pass.
- [x] 2.2 Synchronize the active `decompose-god-classes` design and task evidence with the
  completed responsibility extraction; verify the scoped and umbrella OpenSpec artifacts describe
  the actual structure.
- [x] 2.3 Run formatting, code-size, architecture, public-API, and OpenSpec validation; inspect
  the final diff and archive this no-delta scoped change after the checks pass.
