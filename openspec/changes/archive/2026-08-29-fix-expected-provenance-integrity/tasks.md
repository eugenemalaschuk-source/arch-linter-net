## 1. Fail-closed expected provenance

- [x] 1.1 Add a stable invalid-expected-integrity reason, validate canonical expected provenance during assessment derivation, and verify a matching produced record cannot yield `pass`.

## 2. Contract and PR validation

- [x] 2.1 Validate the OpenSpec delta; run the focused Core tests, public-API lifecycle checks, formatter, lint, and complete OpenSpec validation; inspect the final diff before archiving.
