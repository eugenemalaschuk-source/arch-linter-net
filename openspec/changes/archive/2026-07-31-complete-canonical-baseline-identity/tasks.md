## 1. Inventory and identity model

- [x] 1.1 Add explicit baseline identity capability metadata for every registered contract family and lifecycle/configuration result.
- [x] 1.2 Add a generated/exhaustive NUnit inventory that fails on unclassified families or display-only baseline identity.
- [x] 1.3 Introduce the typed semantic identity input at the execution seam and retain deterministic, non-line-based occurrence assignment.

## 2. Family completion

- [x] 2.1 Audit every `IsIgnored` call site and supply complete semantic dimensions for baseline-capable dependency, contextual, external, method-body, composition, package, framework, cycle, coverage, independence, protected, placement, layout, metadata, policy-consistency, and public-API finding families.
- [x] 2.2 Preserve authored source-set provenance and concrete instance identity for expanded contracts.
- [x] 2.3 Remove production fallback paths that create a baseline-capable identity from only generic display fields.

## 3. Lifecycle and adapters

- [x] 3.1 Classify requalified structured entries as changed, stale, or ambiguous without broadening matching, and preserve metadata only for proven one-to-one lineage.
- [x] 3.2 Route canonical execution identity unchanged through normalized findings, baseline comparison, SARIF, and Testing projections.
- [x] 3.3 Add regression tests for same-named assemblies, repeated calls, source-set instances, package/framework/metadata/public-API families, output ordering, and lifecycle migrations.

## 4. Documentation and validation

- [x] 4.1 Update baseline migration guidance with the review, update/recapture, and prune workflow for requalified identities.
- [x] 4.2 Run focused tests, formatting, repository acceptance, and strict OpenSpec validation.
