## 1. Deterministic metric catalog

- [x] 1.1 Define the closed initial metric catalog and set-cardinality rule,
  including canonical contributor identity, deduplication, ordering, and
  explicit exclusions for formulas and noisy quality metrics.
- [x] 1.2 Define the component dependency and external-group metric subjects,
  direct-edge scope, self-edge/cycle behavior, and contributor semantics.
- [x] 1.3 Define project/assembly footprint, topology-slice type, and public
  contract-surface metric subjects, native units, and authoritative fact
  sources.

## 2. Applicability and boundary review

- [x] 2.1 Define complete native counting-universe evidence, including
  unmapped/ambiguous, stale, empty, generated/test, and multi-targeting cases
  that must become unassessable rather than lower a value.
- [x] 2.2 Verify that the proposal, design, and delta spec reuse topology,
  graph, ownership, public API, and #505 applicability authorities without
  adding schema, evaluator, output, baseline, public API, or runtime scope.

## 3. Validate and synchronize the design-only change

- [x] 3.1 Run strict OpenSpec validation and review the change artifacts for
  complete scenarios and issue acceptance coverage.
- [x] 3.2 Archive the synchronized capability and validate all main OpenSpec
  specifications.
