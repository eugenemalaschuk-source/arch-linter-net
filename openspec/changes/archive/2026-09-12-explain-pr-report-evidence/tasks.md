## 1. Canonical report explanation

- [x] 1.1 Extend the Core PR-report projection with classified non-healthy dimension explanations and safe, optional transport navigation context; verify focused Core projection/reader tests cover pass/debt/degrading/fail/unassessable states.
- [x] 1.2 Render classified advisory explanations separately from blockers and expose full waiver lifecycle counts; verify renderer tests cover #799-style metadata-incomplete debt and 0/1/20/21 detail bounds.

## 2. Immutable drill-down transport

- [x] 2.1 Add the allowlisted report-artifact URL option to `report pr` and construct its trusted run/head-bound value in the read-only CI producer; verify command, integration, and workflow transport tests reject missing, stale, malicious, or disallowed URLs.
- [x] 2.2 Keep the privileged publisher byte-only and add or update fixture coverage proving artifact navigation cannot alter trust validation or comment publication semantics.

## 3. Evidence and documentation

- [x] 3.1 Add a public-safe canonical Health/change fixture representing the #799 reproducer and verify Core/CLI parity against its deterministic Markdown output.
- [x] 3.2 Update PR-report usage and security documentation for bounded comments, full immutable evidence navigation, and unavailable transport context; run scoped documentation checks and record the unrelated full-docs lint evidence block.

## 4. Integration and validation

- [x] 4.1 Synchronize the implementation with both PR-report OpenSpec deltas, run OpenSpec validation, and archive the completed change.
- [x] 4.2 Run focused Core, CLI, workflow/transport, formatter, architecture, security, packed-CLI, and risk-appropriate CI-equivalent validation; inspect the final diff for scope and public API drift.
