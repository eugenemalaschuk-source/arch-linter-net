## 1. Contract hardening

- [x] 1.1 Enforce the documented `subject_kind` × selector-kind matrix in schema, validation, and policy documentation.
- [x] 1.2 Replace delimiter-composed topology selector and edge identities with structural comparison values, including namespace-cache invalidation.

## 2. Regression coverage and CI contracts

- [x] 2.1 Add focused collision, ordering, mutability, and imported-provenance regression tests.
- [x] 2.2 Update the approved Core API baseline, raw-validator ordering, and CLI context schema-version tests.

## 3. Verification and synchronization

- [x] 3.1 Run focused and affected suites, formatting, policy/API/architecture checks, and OpenSpec validation.
- [x] 3.2 Archive the corrective OpenSpec change and verify the synchronized specs.
