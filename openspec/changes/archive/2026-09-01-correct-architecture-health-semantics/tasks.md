## 1. Authority-faithful Health projection

- [x] 1.1 Replace applicability-as-conformance family projection with typed topology, metric, and external finding/evidence projection; verify real failure and clean-receipt projector tests pass.
- [x] 1.2 Extend Health reasons and human/JSON rendering with canonical family, control, policy, and evidence provenance; verify deterministic provenance JSON tests pass.
- [x] 1.3 Project waiver lifecycle records and resolved-only baseline state without reducing lifecycle failures to ordinary debt or resolved entries to new debt; verify stale, metadata-incomplete, active, and resolved regression tests pass.

## 2. Shared orchestration and consumer contracts

- [x] 2.1 Reuse one canonical analysis snapshot/candidate collection for Health validation and persistent baseline comparison while preserving existing debt-gate callers; verify a focused orchestration test proves the shared path.
- [x] 2.2 Update CLI/Testing projection tests, documentation, and reviewed public API snapshots for the additive provenance and receipt changes; verify API approval and focused CLI/Testing tests pass.

## 3. Validation and lifecycle

- [x] 3.1 Run focused Core, CLI, and Testing tests plus formatter, public-API check, strict architecture and docs checks; verify all commands exit successfully.
- [x] 3.2 Run full repository acceptance, archive the OpenSpec change, and validate all specs; verify `make acceptance` and `openspec validate --all --strict` pass.
