## 1. Contract and guidance correction

- [x] 1.1 Record the precise transient and identity-bearing baseline dimensions in the federation
  delta; verify the modified requirement includes a rerun-versus-producer/context scenario.
- [x] 1.2 Update the external-evidence guide to distinguish run ID/artifact provenance from
  producer tool and assessment-context identity; verify the guide has no broad claim that all
  producer provenance avoids baseline churn.

## 2. Validation and lifecycle

- [x] 2.1 Run the existing V2 producer-version/rerun baseline-identity regression test and verify
  it proves both intentional identity changes and transient non-churn.
- [x] 2.2 Validate the OpenSpec change and documentation structure, then archive the completed
  change so the live specification is regenerated without modifying prior archives.
