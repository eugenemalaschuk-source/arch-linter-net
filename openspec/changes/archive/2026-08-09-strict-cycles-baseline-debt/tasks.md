## 1. Strict-cycle baseline evidence

- [x] 1.1 Defer strict-cycle candidate admission until the completed layer graph identifies edges that participate in a cycle.
- [x] 1.2 Preserve existing ignore handling, exact candidate identity, cycle detection results, and deterministic ordering.

## 2. Baseline lifecycle consistency

- [x] 2.1 Make verification treat new baseline candidates as out of sync.

## 3. Regression coverage and validation

- [x] 3.1 Add focused acyclic and cyclic multi-layer Core baseline tests, including update/verify behavior and exact identity.
- [x] 3.2 Add CLI human/JSON/exit-code coverage for new baseline debt.
- [x] 3.3 Run focused tests, formatting, acceptance, and OpenSpec validation.
