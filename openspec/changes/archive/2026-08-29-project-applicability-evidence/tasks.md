## 1. Core applicability projection

- [x] 1.1 Add a Core-owned deterministic control-evaluability summary and typed normalized applicability diagnostic projection from existing completion evidence, preserving canonical identity, membership, state, reasons, provenance, strict/audit mode, and no policy recount; verify focused Core model/projector tests cover required, optional, not-applicable, missing, duplicate, orphan, incompatible, unexpected-empty, unmapped/ambiguous, and stale evidence.
- [x] 1.2 Register the typed applicability diagnostic in every existing normalized-finding/JSON/SARIF subtype boundary and reuse the existing structured identity and baseline/debt candidate lifecycle where it accepts the projection; verify Core normalized-finding and baseline identity tests prove deterministic exact known-versus-new applicability evidence without a display-text key or a trusted-pass bypass.
- [x] 1.3 Preserve expected applicability membership and produced records through analysis-cache serialization/rehydration and derive the same completion/projection on cache hits; verify a focused cache round-trip test has byte-equivalent canonical evidence.

## 2. Output and testing-adapter projection

- [x] 2.1 Wire the Core applicability projection into CLI Human, JSON, and SARIF outputs as additive completion transparency and normalized findings; verify focused CLI tests assert equivalent canonical control identity, summary counts, reason/provenance, and strict/audit severity across formats.
- [x] 2.2 Expose the same typed control projection and normalized findings through `ArchLinterNet.Testing` without parsing formatted output; verify focused Testing-adapter tests cover the generic evidence fixtures and preserve `unassessable` completion despite known-debt identity evidence.
- [x] 2.3 Update public CLI documentation and reviewed Core/Testing public API snapshots for the additive projection contract; verify the documented ratio is explicitly completeness transparency rather than quality scoring.

## 3. Integration and lifecycle verification

- [x] 3.1 Run the risk-appropriate Core, CLI, Testing, cache, normalized-output, and baseline/debt test families plus formatter, OpenSpec, architecture, and public-API checks; inspect the final diff and record exact results before archiving.
- [x] 3.2 Synchronize the finalized OpenSpec deltas with implementation and archive `project-applicability-evidence`; verify `openspec validate --all` passes after archive.
