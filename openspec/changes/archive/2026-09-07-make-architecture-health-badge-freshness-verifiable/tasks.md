## 1. Canonical badge projection

- [x] 1.1 Project canonical Gate before canonical Health in the Architecture Health badge headline while preserving Health-owned color and Gate exit codes; verify all Gate/Health/unassessable handler tests pass.
- [x] 1.2 Extend deterministic CLI golden tests so advisory and blocking degrading states, all Health states, and unavailable counts prove no inferred Gate, color, or counter; verify the focused NUnit filter passes.

## 2. Verifiable static publication

- [x] 2.1 Version the badge publication receipt and atomically record analyzed/merged identities, producer/publisher runs and attempts, digest, status/reason, and publication time; verify the workflow transport-contract tests pass.
- [x] 2.2 Preserve fail-closed stale, missing, corrupt, and compare-and-swap behavior for ready and unavailable receipt paths; verify focused publisher tests cover each rejection path.

## 3. Reader navigation and diagnostics

- [x] 3.1 Link the README Architecture Health badge to its verifiable receipt and document raw-source, Shields, README, and Camo freshness diagnosis without semantic cache busting; verify public-doc checks pass.

## 4. Integration and lifecycle

- [x] 4.1 Run formatter, focused CLI and publisher/doc validation, OpenSpec validation, and the directly implicated workflow lint; verify results and fix issue-related failures.
- [x] 4.2 Synchronize the implemented specifications, archive the completed OpenSpec change, and verify `openspec validate --all` passes before the pull request is opened.
