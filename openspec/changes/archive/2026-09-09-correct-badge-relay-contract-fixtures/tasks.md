## 1. Correct executable contract boundaries

- [x] 1.1 Preserve existing CLI escaped wire bytes and add complete Health-owned color mapping; verify canonical byte digests and wrong-color vectors.
- [x] 1.2 Separate protected JOSE headers from signed claims, add `nbf`, and isolate every OIDC vector's intended rejection cause; verify all time windows.
- [x] 1.3 Align ADR, schema, and vectors on cache-control and complete-representation ETags; verify renewed identical headlines have a distinct validator.

## 2. Validate and archive

- [x] 2.1 Run JSON/schema/digest/vector, documentation, and strict OpenSpec validation; archive the synchronized OpenSpec delta and verify all specs.
