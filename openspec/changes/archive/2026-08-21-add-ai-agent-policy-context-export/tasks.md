## 1. Core policy-context export

- [x] 1.1 Add the versioned Core request/result model and application service that projects effective policy and portable provenance.
- [x] 1.2 Implement deterministic JSON and compact Markdown renderers with bounded reviewed AI guidance and no rooted local paths.
- [x] 1.3 Expose the operation through the established Core composition/facade API without changing validation behavior.

## 2. CLI policy context command

- [x] 2.1 Add the `policy context` command definition, options, help text, and instance handler under the existing policy module.
- [x] 2.2 Extend the CLI runtime seam and error handling to export JSON or Markdown through Core.

## 3. Tests and documentation

- [x] 3.1 Add focused Core tests covering deterministic JSON/Markdown, modular-monolith and Unity/client policy facts, and no-analysis/path-safety behavior.
- [x] 3.2 Add composed-policy provenance and CLI structural/integration tests for both output formats and failure behavior.
- [x] 3.3 Update CLI/output documentation and the reviewed Core public-API snapshot through its explicit update lifecycle.

## 4. Verification and synchronization

- [x] 4.1 Run focused Core and CLI test suites, formatter, relevant architecture/API checks, and OpenSpec validation; resolve issue-related failures.
- [x] 4.2 Run the issue-required repository acceptance gate and record the exact outcomes.
- [x] 4.3 Synchronize the implementation with the proposal/specs, mark tasks complete, and archive the completed OpenSpec change.
