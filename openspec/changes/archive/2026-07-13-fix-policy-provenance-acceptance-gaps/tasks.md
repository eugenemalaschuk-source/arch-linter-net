## 1. Provenance propagation

- [x] 1.1 Bind expanded strict and audit layer-template contracts to their
  source template provenance for execution and consistency paths.
- [x] 1.2 Preserve composed encounter order and original-primary conflict
  semantics in policy diagnostics.

## 2. CLI policy diagnostics

- [x] 2.1 Add a format-aware CLI path for typed import and validation
  exceptions in human, JSON, and SARIF output.

## 3. Regression coverage and validation

- [x] 3.1 Add Core and CLI regression tests for imported schema failures and
  strict/audit layer-template provenance across reporting surfaces.
- [x] 3.2 Run formatting, acceptance, and OpenSpec validation; synchronize and
  archive the change.
