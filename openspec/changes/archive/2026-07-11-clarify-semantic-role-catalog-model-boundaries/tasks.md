## 1. Catalog alignment

- [x] 1.1 Replace the metadata-only assembly attribute YAML and annotation example with a valid role-bearing assembly classification example.
- [x] 1.2 Document the deferred metadata-only assembly-context limitation and its required future model/schema work.
- [x] 1.3 Add explicit single-role and winning-source metadata semantics with concrete related-role examples.
- [x] 1.4 Mark asmdef and package-reference evidence as future guidance rather than active classification sources.

## 2. Validation and lifecycle

- [x] 2.1 Validate catalog YAML examples against the current JSON schema and semantic-classification model.
- [x] 2.2 Run documentation formatting/linting, OpenSpec validation, `make fmt`, and `make acceptance`; document any environment blocker. (`make fmt` is blocked by missing WSL `/bin/bash`; `make acceptance` passed.)
- [x] 2.3 Synchronize and archive the OpenSpec change.
