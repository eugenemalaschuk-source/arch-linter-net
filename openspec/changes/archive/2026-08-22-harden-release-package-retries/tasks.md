## 1. Fail-closed workflow correction

- [x] 1.1 Run Bash explicitly for manifest verification steps that use Bash variable syntax.
- [x] 1.2 Reject duplicate primary package pushes instead of using duplicate-success behavior.

## 2. Evidence and documentation

- [x] 2.1 Add workflow-contract tests for Windows shell selection and partial-publish retry failure.
- [x] 2.2 Document fail-closed partial-publication handling and validate the change.
