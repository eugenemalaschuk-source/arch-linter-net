## 1. Native policy model and validation

- [x] 1.1 Add the opt-in topology YAML model with stable nodes, bounded subject scope, mappings, directional edges, reviewed out-of-scope declarations, and explicit completeness/drift settings.
- [x] 1.2 Add raw and post-deserialization validation for closed selector shapes, referenced layers/nodes, IDs, duplicates, exhaustive scope, and provenance-aware diagnostics.
- [x] 1.3 Add focused Core tests for valid partial/exhaustive declarations, compatibility, deterministic identities, invalid declarations, and the required completeness classification semantics.

## 2. Policy context and weakening composition

- [x] 2.1 Project effective topology policy facts and provenance through the versioned Core policy-context model and deterministic JSON/Markdown formatters.
- [x] 2.2 Extend the existing generic weakening comparison to expose statically provable reviewed topology scope broadening and bounded unknown-containment changes.
- [x] 2.3 Add focused context and policy-weakening tests for imports, deterministic ordering, no-topology compatibility, and exclusion additions/changes.

## 3. Documentation and contract verification

- [x] 3.1 Document the native topology YAML format, boundaries, completeness modes, mappings, reviewed out-of-scope declarations, and migration/weakening implications.
- [x] 3.2 Update the reviewed Core public-API snapshot and verify focused tests, formatting, policy/schema validation, and architecture linting implicated by the change.
