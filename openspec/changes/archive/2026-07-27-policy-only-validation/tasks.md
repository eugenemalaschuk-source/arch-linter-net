## 1. Policy-check Core seam

- [x] 1.1 Add an assembly-free Core policy-check result and application service that loads the policy exactly once.
- [x] 1.2 Expose the Core service through the engine and public validator API with explicit deferred state.

## 2. CLI and Testing API

- [x] 2.1 Add the `policy check` CLI module, help text, human/JSON/SARIF projections, and exit behavior.
- [x] 2.2 Expose the equivalent policy-check operation from the Testing API.

## 3. Documentation and verification

- [x] 3.1 Add focused Core and CLI coverage for valid and invalid policies.
- [x] 3.2 Document policy check and capability metadata, format the solution, and run acceptance.
