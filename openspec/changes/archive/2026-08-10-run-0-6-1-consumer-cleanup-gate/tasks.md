## 1. Synthetic modular consumer fixture

- [x] 1.1 Add a `modular-consumer` adoption fixture with 20 module assemblies, shared
  abstractions, a composition host, and an excluded test project.
- [x] 1.2 Author its composed policy so one directional assembly contract governs every module
  through a source set and project-metadata contracts reuse one solution-discovered project set.
- [x] 1.3 Declare the layer overlap, glob namespace allowance, dependency contract IDs, and
  coverage scopes the F5/F8/F9 regressions need.

## 2. Packed-artifact consumer-cleanup matrix

- [x] 2.1 Add the F1–F11 consumer-cleanup scenarios against the installed candidate tool.
- [x] 2.2 Add the #465 source-set authoring, discovered-project-set, and fail-closed selector
  scenarios, including the enrolment proof for a newly added module.
- [x] 2.3 Add the consumer-policy-shape scenario and emit typed policy-shape evidence.
- [x] 2.4 Add the candidate release-identity consistency scenario.

## 3. Evidence aggregation and release workflow

- [x] 3.1 Reconcile the required scenario inventory with the inventory the gate produces.
- [x] 3.2 Validate policy-shape evidence and emit an explicit PASS/FAIL publication statement.
- [x] 3.3 Update the release workflow budget and package-validation release-identity assertions.

## 4. Release identity and documentation

- [x] 4.1 Advance the public adoption package line to 0.6.1 in the README and docs.
- [x] 4.2 Document the consumer-cleanup gate and its evidence record.

## 5. Verification and spec completion

- [x] 5.1 Run focused tests, `make fmt`, inspect the diff, and run `make acceptance`.
- [x] 5.2 Synchronize specs, validate OpenSpec strictly, and archive the change.
