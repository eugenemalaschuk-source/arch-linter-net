## 1. Canonical report closure

- [x] 1.1 Extract the neutral History canonical scalar comparer from raw Git path decoding and retain existing exact scalar-order behavior.
- [x] 1.2 Add focused conformance coverage for presentation-environment-stable canonical JSON and available/unavailable enrichment non-interference with Git-level output.

## 2. Executable governance

- [x] 2.1 Declare History canonical, Git, configuration, task, analysis, reporting, enrichment, and CLI command layers with strict dependency boundaries.
- [x] 2.2 Include the new strict-rule IDs in self-policy rule-input coverage and verify the real repository policy exercises them.

## 3. Release dogfood evidence

- [x] 3.1 Run the shipped history command against `v0.3.1` to `v0.4.0` using separate operands, with and without requested enrichment.
- [x] 3.2 Record the reproducible release-range summary, manual comparison, intentional v1 limitations, and enrichment non-interference evidence.

## 4. Synchronization and validation

- [x] 4.1 Run focused tests, formatter, strict OpenSpec validation, and the implicated architecture policy gate.
- [x] 4.2 Synchronize and archive the OpenSpec change after the implementation and evidence match the final behavior.
