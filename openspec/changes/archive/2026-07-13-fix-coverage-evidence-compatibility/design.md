## Decisions

- Replace the positional exclusion record with an explicit record retaining both two- and three-argument constructors.
- Emit evidence fields and human suffixes only when evidence is non-empty.
- Reuse the canonical recursive metadata formatter for selector descriptions and identities.
