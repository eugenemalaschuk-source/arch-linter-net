## 1. Decision documentation

- [x] 1.1 Record the first-wave no-built-in-annotations decision in the catalog.
- [x] 1.2 Update the semantic-role-catalog specification with the decision and #108 handoff.

## 2. Validation and lifecycle

- [x] 2.1 Run documentation formatting/linting, OpenSpec validation, `make fmt`, and `make acceptance`; document any environment blocker. (`make fmt` is blocked by missing WSL `/bin/bash`; `make acceptance` passed.)
- [x] 2.2 Synchronize and archive the OpenSpec change.
