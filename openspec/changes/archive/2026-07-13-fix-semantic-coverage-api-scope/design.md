## Decisions

- Use an explicit positional record declaration with an additional optional evidence property.
- Filter conflict and failure diagnostics by their type subject's namespace when semantic roots are declared.
- Move newly added semantic coverage tests to a partial test fixture file.
