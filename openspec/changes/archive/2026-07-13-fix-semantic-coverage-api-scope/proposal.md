# Fix semantic coverage API and scope

## Why

Semantic coverage tests exceed the configured file-size limit, the exclusion evidence model lost positional-record API members, and classification diagnostics can escape a semantic contract's roots.

## What Changes

- Split semantic coverage tests, restore the positional record API, and scope classification diagnostics to contract roots.
