## Decisions

- Reuse semantic summary diagnostic generation for strict coverage violations with stable subject/evidence.
- Extend excluded summary items with optional evidence, preserving backwards-compatible construction.
- Format metadata recursively with invariant culture and deterministic sequence values.
- Sort conflict and failure evidence using every meaningful record field.
