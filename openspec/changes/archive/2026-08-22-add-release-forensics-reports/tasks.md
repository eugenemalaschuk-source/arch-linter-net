## 1. Report input and identity

- [x] 1.1 Retain effective configuration, hotspot findings, and the typed default enrichment projection on successful finalized Core analysis.
- [x] 1.2 Add focused Core tests proving the report input preserves Git-only analysis and default enrichment status.

## 2. Canonical report renderers

- [x] 2.1 Implement the explicit version-1 canonical JSON report schema, configuration identity, and complete finalized-evidence projection.
- [x] 2.2 Implement deterministic, evidence-referencing candidate projection with qualification and caveat coverage.
- [x] 2.3 Implement deterministic Markdown rendering and its required interpretation limits.
- [x] 2.4 Add focused Core golden tests for canonical bytes, reports, candidates, enrichment states, Markdown, and fail-closed separation.

## 3. CLI and documentation

- [x] 3.1 Replace the internal `history ingest` command surface with `history analyze` and JSON/Markdown format selection.
- [x] 3.2 Update focused CLI tests and release-forensics/CLI documentation for the report contract and command.

## 4. Verification and OpenSpec synchronization

- [x] 4.1 Run focused Core and CLI test projects, format changed files, and inspect the diff.
- [x] 4.2 Run OpenSpec validation, synchronize the completed change, archive it, and validate the archived specs.
