---
name: feature-implementation
description: Implement exactly one GitHub issue through the repository's mandatory branch, OpenSpec, implementation, validation, and pull request lifecycle. Use when the user provides a GitHub issue URL, especially when the message contains only that URL.
---

Read and follow this workflow as mandatory instructions:

`../../../docs/ai/feature-implementation-workflow.md`

Treat the single GitHub issue URL in the current user message as `ISSUE_URL` and execute the complete workflow autonomously.

Do not ask the user to repeat the workflow or confirm that implementation should begin. Do not apply this skill to pull request URLs. Do not merge the resulting pull request unless explicitly requested.
