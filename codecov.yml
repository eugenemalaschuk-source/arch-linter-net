coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        # 80% keeps meaningful coverage expectations without penalizing mechanical refactors.
        target: 80%
        threshold: 0%
