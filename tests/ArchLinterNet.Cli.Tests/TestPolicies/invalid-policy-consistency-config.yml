version: 1
name: Test Invalid Policy Consistency Config

layers:
  core:
    namespace: ArchLinterNet.Core

analysis:
  target_assemblies:
    - ArchLinterNet.Core
  policy_consistency: erorr

contracts:
  strict:
    - id: core-no-forbidden
      name: core-has-no-forbidden-dependencies
      source: core
      forbidden: []
      reason: Core has no forbidden dependencies in this test.
