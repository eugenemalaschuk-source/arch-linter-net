{
  "name": "@arch-linter-net/badge-relay",
  "version": "0.1.0",
  "private": true,
  "description": "The isolated badge-relay/v1 Cloudflare Worker runtime.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage.enabled true",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "jose": "6.2.12"
  },
  "overrides": {
    "sharp": "0.35.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.22.0",
    "@cloudflare/workers-types": "5.20260911.1",
    "@vitest/coverage-istanbul": "4.1.11",
    "typescript": "5.9.2",
    "vitest": "4.1.11",
    "wrangler": "4.131.0"
  }
}
